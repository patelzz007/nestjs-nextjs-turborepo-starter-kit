/**
 * Local re-export of the shared `cn` helper so app components can import it
 * from `@/lib/utils` (consistent with the rest of the app's alias imports).
 */
export { cn } from "@workspace/ui/lib/utils";
