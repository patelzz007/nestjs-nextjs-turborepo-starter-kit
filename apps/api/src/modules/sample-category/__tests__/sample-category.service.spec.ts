import { describe, expect, it } from "vitest";

import { SampleCategoryService } from "../sample-category.service";

describe("SampleCategoryService", () => {
	it("is defined", () => {
		expect(SampleCategoryService).toBeDefined();
	});
});
