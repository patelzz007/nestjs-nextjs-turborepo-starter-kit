"use client";

import { ShellThemeToggle } from "@workspace/ui/components/navigation/shell-theme-toggle";
import * as React from "react";

/** Light/dark toggle for merchant shell and login. */
export function MerchantThemeToggle(): React.JSX.Element {
	return <ShellThemeToggle />;
}
