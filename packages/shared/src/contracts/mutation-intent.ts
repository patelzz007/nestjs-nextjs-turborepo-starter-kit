/** Custom header first-party browser clients send on cookie-authenticated mutations. */
export const MUTATION_INTENT_HEADER = "X-Mutation-Intent";

/** Fixed value browsers must send to prove same-origin mutation intent. */
export const MUTATION_INTENT_VALUE = "same-origin";
